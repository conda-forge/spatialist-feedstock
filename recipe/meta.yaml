{% set name = "spatialist" %}
{% set version = "0.20.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: b1a1e330d29eb8d706a33efaf694d2e85a181b0030d3921705195e48e5befe3f

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.11
    - pip
    - setuptools
    - setuptools_scm
  run:
    - dill
    - gdal >=2.4
    - geopandas
    - libspatialite >=5.1.0
    - matplotlib-base
    - numpy
    - pandas
    - pathos >=0.2
    - progressbar2
    - python >=3.11
    - pyyaml
    - setuptools_scm
    - shapely
    - sqlite
    - tblib

test:
  requires:
    - pip
    - python >=3.11
  imports:
    - spatialist
  commands:
    - pip check

about:
  home: https://github.com/johntruckenbrodt/spatialist
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: A Python module for spatial data handling

  description: |
    This package offers functionalities for user-friendly geo data processing using GDAL and OGR.
  doc_url: https://spatialist.readthedocs.io
  dev_url: https://github.com/johntruckenbrodt/spatialist

extra:
  recipe-maintainers:
    - johntruckenbrodt
